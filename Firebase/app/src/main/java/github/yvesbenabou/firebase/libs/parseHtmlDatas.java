package github.yvesbenabou.firebase.libs;

import github.yvesbenabou.firebase.MainActivity.Liste_Salles;
import github.yvesbenabou.firebase.libs.*;

public class parseHtmlDatas {
    public static Liste_Salles parsing(String html, String time) {
        // Parse the HTML data and extract the necessary information (e.g., room numbers, states, positions)
        // You can use libraries like Jsoup or HtmlUnit to parse the HTML
        // Then, populate the Salle objects with the extracted data

        Liste_Salles liste = new Liste_Salles();

        //Parsing du code HTML de la page ADE


        return liste;
    }
}
